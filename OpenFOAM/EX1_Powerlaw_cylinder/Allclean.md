```bash
# Clean case
rm -rf [1-9]* 0.*
rm -rf processor* # will be created when we'll unlock parallel computing with our fellow Giancarlo i can't remember
rm -rf dynamicCode # this will be needed to declare variables and make mathematical operation in an openfoam file, you see something like #calc "$a + $b"
rm -rf constant/polyMesh
```
Ok, he just explained wildcards. 